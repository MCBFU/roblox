-- 不是,你怎么看到的？Σ( ° △ °|||)︴
-- 就当什么都没看见,返回 好吗






























local success, Library = pcall(function()
    return loadstring(game:HttpGet("https://gitee.com/cmbhbh/ycgui/raw/master/YCmain.lua"))()
end)

if not success then
    warn("playbreak加载失败！请更换有效链接，推荐备用链接：https://raw.githubusercontent.com/zzerexx/scripts/main/yc.lua")
    return
end

-- ========== 1. 先创建UI结构（顺序绝对不能乱） ==========
local Main = Library:CreateMainControl("playbreak")
local CombatWin = Library:CreateChildWindow("Monster") -- 全功能窗口
local VisualsWin = Library:CreateChildWindow("Expert")

Main:BindWindow("Monster", false)
Main:BindWindow("Expert", false)

-- ========== 2. 全局变量与核心服务 ==========
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local Workspace = game:GetService("Workspace")
local SoundService = game:GetService("SoundService")
local TweenService = game:GetService("TweenService")

local player = Players.LocalPlayer
local camera = Workspace.CurrentCamera
local isAuraActive = false
local teleportInProgress = false  -- 重命名为更通用的变量名
local renderConnection

-- 可配置参数（滑块控制）
local Settings = {
    auraDistance = 300,      -- 攻击距离
    attackInterval = 0.1,    -- 攻击间隔
    teleportHeight = 0.5,      -- 传送高度补偿 修复：0易卡地，改为5
    teleportSpeed = 0.1      -- 每次传送间隔（重命名）
}

-- 血量保护相关变量
local isHealthProtectionActive = false
local healthProtectionThreshold = 0.3 -- 默认30%血量触发
local healthProtectionConnection = nil
local lastHealth = 100
local maxHealth = 100
local lastTeleportHealth = 100  -- 新增：记录上次传送时的血量

-- 传送坐标定义
local teleportLocations = {
    blackRoom = {X = 906, Y = 24, Z = 377, Name = "小黑屋"},
    backroom = {X = 163.15, Y = 9, Z = 955.4, Name = "等候厅"},
    theaterFactory = {X = 0, Y = -297, Z = 0, Name = "剧院/工厂"},
    sewer = {X = 356, Y = 528, Z = -179, Name = "下水道"}
}

-- 存储所有可互动物体的位置
local interactableLocations = {}

-- 修复：补全缺失的fireclickdetector函数
local function fireclickdetector(clickdetector)
    if clickdetector and clickdetector:IsA("ClickDetector") then
        local args = {
            [1] = player,
            [2] = 1
        }
        pcall(function()
            clickdetector.MouseClick:InvokeServer(unpack(args))
        end)
    end
end

-- 自动搜索所有可互动物体
local function findAllInteractables()
    table.clear(interactableLocations)

    -- 搜索 ClickDetector
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj:IsA("ClickDetector") then
            local part = obj.Parent
            if part and part:IsA("BasePart") then
                table.insert(interactableLocations, part.Position)
            end
        end
    end

    -- 搜索 ProximityPrompt
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj:IsA("ProximityPrompt") then
            local part = obj.Parent
            if part and part:IsA("BasePart") then
                table.insert(interactableLocations, part.Position)
            end
        end
    end

    -- 搜索可互动的按钮
    for _, obj in pairs(Workspace:GetDescendants()) do
        if obj:IsA("TextButton") or obj:IsA("ImageButton") then
            -- 查找按钮的位置
            local buttonFrame = obj:FindFirstAncestorOfClass("SurfaceGui") or 
                               obj:FindFirstAncestorOfClass("BillboardGui") or 
                               obj:FindFirstAncestorOfClass("BasePart")
            if buttonFrame and buttonFrame:IsA("BasePart") then
                table.insert(interactableLocations, buttonFrame.Position)
            end
        end
    end

    print("找到", #interactableLocations, "个可互动物体")
end

-- ========== 3. 辅助函数 ==========
-- 稳定获取根部件
local function getRootPart()
    local char = player.Character or player.CharacterAdded:Wait(5)
    if not char then return nil end
    return char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Torso")
end

-- 播放音效 修复：音效ID格式错误
local function playSound()
    local sound = Instance.new("Sound")
    sound.SoundId = "rbxassetid://984469981"
    sound.Parent = SoundService
    sound:Play()
    sound.Ended:Connect(function() sound:Destroy() end)
end

-- 缺失的通知函数（空实现，不弹出提示）
local function createExpertNotification(text, color)
    -- 空函数，不发送任何提示
end

-- 通用传送函数
local function performTeleport(location)
    local character = player.Character
    if not character then 
        return false 
    end

    local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
    if not humanoidRootPart then 
        return false 
    end

    local teleportPos = Vector3.new(location.X, location.Y + Settings.teleportHeight, location.Z)
    humanoidRootPart.CFrame = CFrame.new(teleportPos)
    return true
end

-- Expert传送函数
local function performExpertTeleport(location, locationName)
    return performTeleport(location)
end

-- ========== 血量保护传送函数（修复版：持续监控） ==========
local function performHealthProtectionTeleport()
    -- 使用等候厅坐标 (163.15, 9, 955.4) 代替 (0, 100, 0)
    return performTeleport(teleportLocations.backroom)
end

-- 检查血量并触发保护（修复版：持续监控）
local function checkHealthProtection()
    local character = player.Character
    if not character then return end

    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if not humanoid then return end

    local currentHealth = humanoid.Health
    maxHealth = humanoid.MaxHealth

    -- 计算血量百分比
    local healthPercent = currentHealth / maxHealth

    -- 如果血量低于阈值
    if healthPercent < healthProtectionThreshold then
        -- 检查是否需要传送（避免连续传送）
        local shouldTeleport = false

        -- 条件1：血量低于阈值且上次传送时血量较高（至少低5%血量才再次传送）
        local healthDifference = lastTeleportHealth - currentHealth
        local healthDifferencePercent = healthDifference / maxHealth

        if currentHealth < lastHealth and healthDifferencePercent > 0.05 then
            shouldTeleport = true
            Library:Notify("已传送", true)
            Library:Notify("不客气", true)
        -- 条件2：血量已经很低（低于20%），直接传送
        elseif healthPercent < 0.2 then
            shouldTeleport = true
            Library:Notify("已传送！", true)
            Library:Notify("感谢使用", true)
        end

        if shouldTeleport then
            -- 触发保护传送
            local teleported = performHealthProtectionTeleport()
            if teleported then
                lastTeleportHealth = currentHealth  -- 更新上次传送时的血量
            end
        end
    -- 如果血量恢复到阈值以上，重置传送记录
    elseif healthPercent >= healthProtectionThreshold + 0.1 then  -- 比阈值高10%时重置
        lastTeleportHealth = currentHealth
    end

    -- 更新血量记录
    lastHealth = currentHealth
end

-- 开启/关闭血量保护
local function toggleHealthProtection(state)
    isHealthProtectionActive = state

    if isHealthProtectionActive then
        -- 开启保护
        if healthProtectionConnection then
            healthProtectionConnection:Disconnect()
        end

        healthProtectionConnection = RunService.Heartbeat:Connect(function()
            checkHealthProtection()
        end)

        -- 初始化血量记录
        local character = player.Character
        if character then
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                lastHealth = humanoid.Health
                maxHealth = humanoid.MaxHealth
                lastTeleportHealth = lastHealth  -- 初始化上次传送血量
            end
        end
    else
        -- 关闭保护
        if healthProtectionConnection then
            healthProtectionConnection:Disconnect()
            healthProtectionConnection = nil
        end
        -- 重置传送记录
        lastTeleportHealth = 100
    end
end

-- ========== 5. Expert功能 - 仙人模式（优化版，无提示） ==========
local isNinjaModeActive = false
local originalWalkSpeed = 16
local originalJumpPower = 50
local ninjaModeMultiplier = 2.5
local infiniteJumpEnabled = false
local noclipEnabled = false
local ninjaModeConnection = nil
local noclipConnection = nil

-- 穿墙功能（优化版） 修复：补全残缺函数头
local function enableNoclip()
    if noclipConnection then return end
    noclipConnection = RunService.Stepped:Connect(function()
        local character = player.Character
        if character and noclipEnabled and isNinjaModeActive then
            for _, part in pairs(character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
    end)
end

local function disableNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
        -- 恢复碰撞
        local character = player.Character
        if character then
            for _, part in pairs(character:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = true
                end
            end
        end
    end
end

local function startNinjaMode()
    if isNinjaModeActive then return end

    local character = player.Character
    if not character then 
        return false 
    end

    local humanoid = character:FindFirstChild("Humanoid")
    if not humanoid then 
        return false 
    end

    originalWalkSpeed = humanoid.WalkSpeed
    originalJumpPower = humanoid.JumpPower
    isNinjaModeActive = true
    infiniteJumpEnabled = true
    noclipEnabled = true

    humanoid.WalkSpeed = originalWalkSpeed * ninjaModeMultiplier
    humanoid.JumpPower = originalJumpPower * 3

    -- 无限跳跃（优化防抽搐）
    ninjaModeConnection = UserInputService.JumpRequest:Connect(function()
        if infiniteJumpEnabled and character and character:FindFirstChild("Humanoid") and character.Humanoid.FloorMaterial == Enum.Material.Air then
            character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end)

    enableNoclip()
    return true
end

local function stopNinjaMode()
    if not isNinjaModeActive then return end
    isNinjaModeActive = false
    infiniteJumpEnabled = false
    noclipEnabled = false

    -- 先关闭跳跃
    if ninjaModeConnection then
        ninjaModeConnection:Disconnect()
        ninjaModeConnection = nil
    end

    -- 然后关闭穿墙
    disableNoclip()

    -- 最后恢复属性（防止抽搐）
    task.wait(0.05)

    local character = player.Character
    if character then
        local humanoid = character:FindFirstChild("Humanoid")
        if humanoid then
            humanoid.WalkSpeed = originalWalkSpeed
            humanoid.JumpPower = originalJumpPower
        end
    end
end

-- ========== 角色重生处理 ==========
player.CharacterAdded:Connect(function(character)
    task.wait(0.5)

    -- 恢复仙人模式
    if isNinjaModeActive then
        local humanoid = character:FindFirstChild("Humanoid")
        if humanoid then
            humanoid.WalkSpeed = originalWalkSpeed * ninjaModeMultiplier
            humanoid.JumpPower = originalJumpPower * 3
            if noclipEnabled then
                enableNoclip()
            end
        end
    end

    -- 恢复血量保护
    if isHealthProtectionActive then
        local humanoid = character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            lastHealth = humanoid.Health
            maxHealth = humanoid.MaxHealth
            lastTeleportHealth = lastHealth  -- 重置上次传送血量
        end
    end
end)

-- ========== 6. Monster窗口功能 ==========
-- 6.1 杀戮光环（保持原有逻辑+修复断开连接，无提示）
local function auraMainLoop()
    local root = getRootPart()
    if not root or not isAuraActive then return end

    local nearestTarget, minDist = nil, math.huge
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= player and p.Character then
            local tRoot = p.Character:FindFirstChild("HumanoidRootPart")
            local hum = p.Character:FindFirstChild("Humanoid")
            if tRoot and hum and hum.Health > 0 then
                local dist = (root.Position - tRoot.Position).Magnitude
                if dist < Settings.auraDistance and dist < minDist then
                    minDist = dist
                    nearestTarget = tRoot
                end
            end
        end
    end

    if nearestTarget then
        root.CFrame = CFrame.new(nearestTarget.Position.X, nearestTarget.Position.Y + Settings.teleportHeight, nearestTarget.Position.Z)
        camera.CFrame = CFrame.lookAt(camera.CFrame.Position, nearestTarget.Position)
        if tick() % Settings.attackInterval < 0.01 then
            VirtualInputManager:SendMouseButtonEvent(0,0,0,true,game,0)
            task.wait(0.005)
            VirtualInputManager:SendMouseButtonEvent(0,0,0,false,game,0)
        end
    end
end

local function toggleAura(state)
    isAuraActive = state
    if state then
        renderConnection = RunService.RenderStepped:Connect(auraMainLoop)
        playSound()
    else
        if renderConnection then 
            renderConnection:Disconnect()
            renderConnection = nil
        end
    end
end

-- 6.2 致命管道（新增功能）
local function performAutoInteractableTeleport()
    if teleportInProgress then
        return
    end
    teleportInProgress = true

    local root = getRootPart()
    if not root then
        teleportInProgress = false
        return
    end

    local originalCFrame = root.CFrame

    -- 先搜索所有可互动物体
    findAllInteractables()

    if #interactableLocations == 0 then
        teleportInProgress = false
        return
    end

    task.spawn(function()
        for i, pos in ipairs(interactableLocations) do
            local r = getRootPart()
            if not r or not teleportInProgress then break end

            -- 传送并互动
            r.CFrame = CFrame.new(pos.X, pos.Y + Settings.teleportHeight, pos.Z)

            -- 尝试互动
            -- 查找该位置的互动物体
            for _, obj in pairs(Workspace:GetDescendants()) do
                if obj:IsA("BasePart") and (obj.Position - pos).Magnitude < 5 then
                    local clicker = obj:FindFirstChildOfClass("ClickDetector")
                    if clicker then
                        fireclickdetector(clicker)
                    end

                    local prompt = obj:FindFirstChildOfClass("ProximityPrompt")
                    if prompt then
                        pcall(function()
                            prompt.HoldDuration = 0
                            prompt:InputHoldBegin()
                            task.wait(0.001)
                            prompt:InputHoldEnd()
                        end)
                    end
                end
            end

            task.wait(Settings.teleportSpeed)
        end

        -- 传送回原位置
        local r = getRootPart()
        if r then r.CFrame = originalCFrame end

        teleportInProgress = false
    end)
end

-- 6.3 轮流传送（无提示）
local cycleTeleportIndex = 1
local function performCycleTeleport()
    if teleportInProgress then
        return
    end
    local root = getRootPart()
    if not root then
        return
    end

    local validPlayers = {}
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= player and p.Character then
            local tRoot = p.Character:FindFirstChild("HumanoidRootPart")
            local hum = p.Character:FindFirstChild("Humanoid")
            if tRoot and hum and hum.Health > 0 then
                table.insert(validPlayers, p)
            end
        end
    end

    if #validPlayers == 0 then
        return
    end

    cycleTeleportIndex = cycleTeleportIndex % #validPlayers + 1
    local target = validPlayers[cycleTeleportIndex]
    local tRoot = target.Character:FindFirstChild("HumanoidRootPart")
    root.CFrame = CFrame.new(tRoot.Position.X, tRoot.Position.Y + Settings.teleportHeight, tRoot.Position.Z)
end

-- 6.4 新增：传送小黑屋功能
local function teleportToBlackRoom()
    return performTeleport(teleportLocations.blackRoom)
end

-- ========== 7. 播放Expert音效 ==========
local function playExpertSound()
    local success, soundError = pcall(function()
        local expertSound = Instance.new("Sound")
        expertSound.SoundId = "rbxassetid://7390036132"
        expertSound.Parent = SoundService
        expertSound:Play()
        expertSound.Ended:Connect(function() expertSound:Destroy() end)
    end)

    if not success then
        warn("Expert音频加载失败，但功能正常可用")
    end
end

-- ========== 9. 创建Monster窗口功能模块 ==========
-- 只保留攻击距离滑块和每次传送间隔滑块
local Aura = CombatWin:CreateModule("杀戮光环", toggleAura)
Aura:CreateSlider("攻击距离", 1, 500, 300, function(val)
    Settings.auraDistance = val
end)

local TeleportAll = CombatWin:CreateModule("致命管道", performAutoInteractableTeleport)  -- 重命名
TeleportAll:CreateSlider("每次传送间隔", 0.01, 1.0, 0.1, function(val)
    Settings.teleportSpeed = val
end)

CombatWin:CreateModule("轮流传送", performCycleTeleport)

-- 新增：传送小黑屋功能到Monster窗口
CombatWin:CreateModule("传送小黑屋", teleportToBlackRoom)

-- ========== 10. 创建Expert窗口功能模块 ==========
VisualsWin:CreateModule("传送等候厅", function()
    performExpertTeleport(teleportLocations.backroom, teleportLocations.backroom.Name)
end)

VisualsWin:CreateModule("万能传送", function()
    performExpertTeleport(teleportLocations.theaterFactory, teleportLocations.theaterFactory.Name)
end)

VisualsWin:CreateModule("传送下水道", function()
    performExpertTeleport(teleportLocations.sewer, teleportLocations.sewer.Name)
end)

local NinjaModule = VisualsWin:CreateModule("仙人模式", function(state)
    if state then
        startNinjaMode()
    else
        stopNinjaMode()
        print("看什么看,没见过输出啊😡")
    end
end)

-- ========== 新增：血量保护模块 ==========
local HealthProtectModule = VisualsWin:CreateModule("受伤自动传送", toggleHealthProtection)

-- 为血量保护模块添加滑块
HealthProtectModule:CreateSlider("传送的满足血量", 1, 99, 30, function(val)
    healthProtectionThreshold = val / 100
end)

-- ========== 11. 快捷键控制 ==========
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if not gameProcessed then
        -- 右Ctrl开关杀戮光环
        if input.KeyCode == Enum.KeyCode.RightControl then
            isAuraActive = not isAuraActive
            toggleAura(isAuraActive)

        -- Expert功能快捷键
        elseif input.KeyCode == Enum.KeyCode.B then
            performExpertTeleport(teleportLocations.backroom, teleportLocations.backroom.Name)
        elseif input.KeyCode == Enum.KeyCode.T then
            performExpertTeleport(teleportLocations.theaterFactory, teleportLocations.theaterFactory.Name)
        elseif input.KeyCode == Enum.KeyCode.S then
            performExpertTeleport(teleportLocations.sewer, teleportLocations.sewer.Name)
        elseif input.KeyCode == Enum.KeyCode.N then
            if isNinjaModeActive then
                stopNinjaMode()
            else
                startNinjaMode()
            end
        -- 新增：F键快速传送所有互动物体
        elseif input.KeyCode == Enum.KeyCode.F then
            performAutoInteractableTeleport()
        -- 新增：H键开关血量保护
        elseif input.KeyCode == Enum.KeyCode.H then
            isHealthProtectionActive = not isHealthProtectionActive
            toggleHealthProtection(isHealthProtectionActive)
        -- 新增：K键传送到小黑屋
        elseif input.KeyCode == Enum.KeyCode.K then
            teleportToBlackRoom()
        end
    end
end)

-- ========== 新增：自杀选项（无提示） ==========
VisualsWin:CreateModule("自杀", function()
    local character = player.Character
    if not character then
        return 
    end

    local humanoid = character:FindFirstChild("Humanoid")
    if not humanoid then 
        return 
    end

    -- 直接设置血量为0
    humanoid.Health = 0
end)

-- ========== 12. 初始化 ==========
-- 初始搜索互动物体
task.spawn(function()
    task.wait(2)
    findAllInteractables()
    print("playbreak 初始化完成")
    print("找到可互动物体:", #interactableLocations)
end)

-- 播放Expert加载音效
task.wait(0.5)
playExpertSound()

-- 设置playbreak
Library:SetupSettings()

-- 完整搜索所有GUI
local translation = {
    ["By BeiHai"] = "BY | 渔炒鱼",
    ["YC GUI"] = "PB最强脚本",
}

-- 检查三个地方
local places = {
    game.Players.LocalPlayer:WaitForChild("PlayerGui"),
    game:GetService("CoreGui"),
    game:GetService("StarterGui")
}

for _, gui in pairs(places) do
    for _, obj in pairs(gui:GetDescendants()) do
        if obj:IsA("TextLabel") or obj:IsA("TextButton") or obj:IsA("TextBox") then
            local text = obj.Text
            if text then
                for eng, chi in pairs(translation) do
                    if text == eng then
                        obj.Text = chi
                        break
                    elseif text:find(eng, 1, true) then
                        obj.Text = text:gsub(eng, chi)
                        break
                    end
                end
            end
        end
    end
end

-- 新增：防卡死处理（强制关闭所有连接）
game.Close:Connect(function()
    if renderConnection then renderConnection:Disconnect() end
    if ninjaModeConnection then ninjaModeConnection:Disconnect() end
    if noclipConnection then noclipConnection:Disconnect() end
    if healthProtectionConnection then healthProtectionConnection:Disconnect() end
    teleportInProgress = false
    isAuraActive = false
    isNinjaModeActive = false
    isHealthProtectionActive = false
end) 

task.wait(2) 
Library:Notify("欢迎使用PB最强脚本", true)
Wait(2)
Library:Notify("滥用后果严重", true)
Library:Notify("本人概不负责", true)
Library:Notify("只提供使用,不提供保障", true)